# RailGuard-FSL++ — backend dependencies (pip path; matches pyproject.toml)
# Install:  python -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
# (Windows: .venv\Scripts\activate)

# --- deep learning ---
torch>=2.0.0
torchvision>=0.15.0

# --- numerics / classical ML ---
numpy>=1.24.0
scipy>=1.11.0
scikit-learn>=1.3.0

# --- vision / image IO ---
opencv-python>=4.8.0
Pillow>=10.0.0
albumentations>=1.3.0
imagehash>=4.3.0

# --- API server ---
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.5.0
python-multipart>=0.0.6

# --- utilities ---
tqdm>=4.65.0

# --- dev / tests (optional) ---
pytest>=7.4.0
